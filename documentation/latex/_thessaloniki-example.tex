\doxysection{Thessaloniki}
\hypertarget{_thessaloniki-example}{}\label{_thessaloniki-example}Generate an area code for a fixed line number.

Doesn\textquotesingle{}t include codes for Greater Athens Metropolitan Area (21\#) because this zone uses 3 digits, and phone numbers have a different formatting.

Area codes in all the other zones use 4 digits. The capital of each zone uses 3 digits and the 4th digit can be any number. The other areas in each zone use 4 digits, but not every number is valid for the 4th digit.

has code \textquotesingle{}231\textquotesingle{}, so \textquotesingle{}2310\textquotesingle{} and \textquotesingle{}2313\textquotesingle{} are valid.


\begin{DoxyCodeInclude}{0}

\end{DoxyCodeInclude}
 